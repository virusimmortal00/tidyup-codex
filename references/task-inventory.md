# Local Codex task inventory

Use this only when the app's open-task listing reaches its cap. The sidebar listing is a view, not a complete inventory. On the local Codex host checked on 2026-09-23, `~/.codex/state_5.sqlite` contained a `threads` table with `id`, `archived`, `cwd`, Git metadata, and task recency; the app's global state contained `thread-project-assignments`. The SQLite database listed older unarchived IDs outside the 50 ordinary tasks returned by `list_threads`, and `read_thread` accepted one such ID. These are observed internal files, not a stable public API; inspect the current schema before relying on them.

Read local state without modifying it (for SQLite, open with `mode=ro`). Avoid dumping unrelated task text or credentials. Determine the exact host and saved project ID first. Build candidate IDs from project assignments and canonical repository paths, including linked worktrees and Git origin metadata. Do not treat a matching project name or branch name alone as ownership. Cross-check that every local Codex ID in the app's visible page and pinned list occurs in the local inventory with a consistent archive flag. Check a candidate older than the visible page with `read_thread`. That overlap is necessary but does not alone prove completeness: inspect the storage scope and reconcile any other available indexes or app state. On the inspected host, `session_index.jsonl` had fewer entries than the SQLite `threads` table, so do not treat it alone as a full inventory. A local database does not enumerate another host.

Use the internal inventory to discover IDs, then use app tools such as `read_thread` for each decision and `set_thread_archived` for mutations. Never write to Codex's state files directly. If the schema, project mapping, host coverage, or cross-check is inconclusive, mark that part of the inventory incomplete and use the skill's conservative batch fallback.
